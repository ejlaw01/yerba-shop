<div class="item-detail">
  <h1>{{model.name}}</h1>
  <h2>{{model.typeDetail}} {{model.type}}</h2>
  <img src="{{model.image}}">
  <p>{{model.details}}</p>
  <button class='btn' {{action 'addToCart' model}}>add to cart</button>
  {{new-review item=model saveReview="saveReview"}}
  <hr>
  {{review-tile item=model}}
</div>
